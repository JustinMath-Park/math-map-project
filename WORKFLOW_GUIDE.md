# 📋 Mathiter Workflow 사용 가이드

## 목차
1. [Workflow 구조 개요](#workflow-구조-개요)
2. [5단계 프로세스 상세 설명](#5단계-프로세스-상세-설명)
3. [실행 방법](#실행-방법)
4. [Jira 연동 설정](#jira-연동-설정)
5. [실제 사용 예시](#실제-사용-예시)
6. [문제 해결](#문제-해결)

---

## Workflow 구조 개요

### 전체 흐름도

```
┌─────────────────────────────────────────────────────────────┐
│                    Workflow 5단계 프로세스                    │
└─────────────────────────────────────────────────────────────┘

  Stage 1         Stage 2         Stage 3         Stage 4         Stage 5
    📋              💻              🔍              📧              🚀
 기획 &          코드             AI              사용자           배포 &
Jira 티켓         개발           코드 리뷰          승인           문서화
   |               |               |               |               |
   v               v               v               v               v
[대화형]        [Claude]        [AI 리뷰]       [수동 확인]      [자동화]
Jira 티켓       기능 구현       보안/품질        변경사항         Git 커밋
생성            완료            체크             검토             푸시
                                                                 Confluence
                                                                 Jira 완료
```

### 주요 구성 요소

**1. workflow.py** - 메인 오케스트레이터
- 5단계 프로세스를 순차 실행
- 각 단계에서 사용자 입력 대기
- Git, Jira 자동화

**2. config/workflow_config.json** - 설정 파일
- Jira 연동 정보
- Confluence 연동 정보
- GitHub 저장소 정보
- 프로젝트 메타데이터

**3. lib/jira_client.py** (선택사항)
- Jira API 래퍼 클래스
- 티켓 생성, 상태 변경 자동화

**4. lib/confluence_client.py** (선택사항)
- Confluence API 래퍼 클래스
- 문서 자동 생성

---

## 5단계 프로세스 상세 설명

### Stage 1: 📋 기획 & Jira 티켓 생성

**목적**: 기능 요구사항 정리 및 Jira 티켓 생성

**프로세스**:
1. 기능 설명 입력 (예: "학생 진도 대시보드")
2. Claude Code와 대화형 기획
   - 필요한 UI 컴포넌트?
   - 다루는 데이터?
   - 백엔드 API?
   - 테스트 계획?
3. Jira 티켓 자동 생성 (KAN-XXX)
4. 티켓 URL 반환

**사용자 액션**:
- Enter 키를 눌러 다음 단계로 진행

**현재 상태**:
- ⚠️ Jira API 연동은 TODO 상태 (현재는 더미 티켓 KAN-XXX 반환)

---

### Stage 2: 💻 코드 개발

**목적**: 실제 기능 구현

**프로세스**:
1. 프로젝트 구조 분석
   - Backend: `/Users/justinminim4/projects/backend`
   - Frontend: `/Users/justinminim4/projects/frontend`
2. Claude Code에게 개발 요청
   ```
   "학생 진도 대시보드 기능을 구현해줘.
   Jira 티켓: KAN-XXX
   백엔드는 Flask 패턴 따라서,
   프론트엔드는 기존 스타일 유지해서 만들어줘"
   ```
3. 코드 작성 완료 후 Enter
4. `git status --short`로 변경 파일 자동 감지

**출력 예시**:
```
📝 변경된 파일을 확인합니다...
   📄 backend/routes/dashboard_routes.py
   📄 backend/services/dashboard_service.py
   📄 frontend/dashboard.html
```

**사용자 액션**:
- Claude Code에게 기능 구현 요청
- 구현 완료 후 Enter

---

### Stage 3: 🔍 AI 코드 리뷰

**목적**: 보안 및 품질 검증

**프로세스**:

#### 라운드 1: 보안 & 성능 리뷰
Claude Code에게 요청:
```
"변경된 코드를 보안 관점에서 리뷰해줘.
- SQL Injection, XSS 체크
- N+1 쿼리 문제
- API 보안 (CORS, Rate Limiting)"
```

**체크 항목**:
- [ ] SQL Injection 취약점
- [ ] XSS (Cross-Site Scripting) 취약점
- [ ] CSRF 토큰
- [ ] N+1 쿼리 문제
- [ ] CORS 설정
- [ ] Rate Limiting

#### 라운드 2: 코드 품질 리뷰
Claude Code에게 요청:
```
"코드 품질 관점에서 리뷰해줘.
- PEP 8 준수
- 타입 힌팅
- Flask 패턴"
```

**체크 항목**:
- [ ] PEP 8 코딩 스타일
- [ ] 타입 힌팅 (typing 모듈)
- [ ] Docstring 작성
- [ ] Flask Blueprint 패턴
- [ ] 에러 핸들링

**사용자 액션**:
- 각 라운드 리뷰 후 Enter
- 최종 확인: "모든 리뷰를 통과했나요? (y/n)"

---

### Stage 4: 📧 사용자 승인

**목적**: 최종 검토 및 배포 승인

**프로세스**:
1. 변경 파일 목록 출력
2. 사용자 검토 요청
   - 로컬에서 코드 확인
   - 필요 시 수정
   - 테스트 실행
3. 승인 여부 입력

**출력 예시**:
```
📋 Jira: KAN-123
📝 변경된 파일 (3개):
   - backend/routes/dashboard_routes.py
   - backend/services/dashboard_service.py
   - frontend/dashboard.html

🔍 변경사항을 검토하세요:
   1. 로컬에서 코드 확인
   2. 필요 시 수정
   3. 테스트 실행

   ✅ 배포를 승인하시겠습니까? (y/n):
```

**사용자 액션**:
- 코드 검토
- `y` 입력 → Stage 5로 진행
- `n` 입력 → 워크플로우 취소

---

### Stage 5: 🚀 배포 & 문서화

**목적**: Git 커밋, GitHub 푸시, Confluence 문서화, Jira 완료

**프로세스**:

#### 1. Git 작업
```bash
# 브랜치 생성
feature/kan-xxx-학생-진도-대시보드

# 커밋 메시지
feat: 학생 진도 대시보드 (KAN-XXX)
```

**사용자 액션**:
- "Git 커밋을 생성하시겠습니까? (y/n)": `y`
- "GitHub에 푸시하시겠습니까? (y/n)": `y`

#### 2. Confluence 문서화
Claude Code에게 요청:
```
"KAN-XXX에 대한 기술 문서를 작성해서
Confluence에 업로드해줘"
```

**문서 포함 내용**:
- 기능 개요
- API 엔드포인트
- 데이터베이스 스키마
- 사용 방법
- 테스트 가이드

#### 3. Jira 상태 업데이트
```
Testing → Done
```

---

## 실행 방법

### 방법 1: 커맨드라인에서 직접 실행

```bash
# 기본 사용법
python3 workflow.py "기능 설명"

# 예시
python3 workflow.py "학생 진도 대시보드 추가"
python3 workflow.py "문제 필터링 기능 개선"
python3 workflow.py "이메일 알림 시스템 구축"
```

### 방법 2: Claude Code에서 실행 (권장)

Claude Code에서 다음과 같이 요청:

```
"workflow.py를 실행해서 학생 진도 대시보드 기능을 개발하자"
```

또는

```
"워크플로우 스크립트로 문제 자동 생성 기능을 만들어줘"
```

### 실행 흐름 예시

```bash
$ python3 workflow.py "학생 진도 대시보드"

============================================================
  🚀 Mathiter 워크플로우 시작
============================================================

📋 기능: 학생 진도 대시보드

============================================================
  📋 Stage 1: 기획 및 Jira 티켓 생성
============================================================

💬 Claude와 기획 논의를 진행합니다...
   기능: 학생 진도 대시보드

   Claude Code에서 다음 질문들에 대해 논의하세요:
   1. 어떤 UI가 필요한가요?
   2. 어떤 데이터를 다루나요?
   3. 백엔드 API가 필요한가요?
   4. 테스트는 어떻게 하나요?

   ⏸️  기획 논의가 완료되면 Enter를 눌러주세요...
```

---

## Jira 연동 설정

### 현재 상태
- ⚠️ **Jira API는 아직 완전히 구현되지 않음**
- 설정 파일은 준비되어 있음 (`config/workflow_config.json`)
- 티켓 생성은 더미 값 반환 (KAN-XXX)

### Jira API 연동 활성화 방법

#### 1. Jira API 토큰 확인
현재 설정된 토큰:
```json
{
  "jira": {
    "base_url": "https://sspark222.atlassian.net",
    "project_key": "KAN",
    "email": "sspark222@gmail.com",
    "api_token": "ATATT3x..."
  }
}
```

#### 2. lib/jira_client.py 구현 (TODO)

```python
# lib/jira_client.py
import requests
from typing import Dict

class JiraClient:
    def __init__(self, config: Dict):
        self.base_url = config['base_url']
        self.email = config['email']
        self.api_token = config['api_token']
        self.project_key = config['project_key']

    def create_issue(self, summary: str, description: str) -> str:
        """Jira 티켓 생성"""
        url = f"{self.base_url}/rest/api/3/issue"

        payload = {
            "fields": {
                "project": {"key": self.project_key},
                "summary": summary,
                "description": {
                    "type": "doc",
                    "version": 1,
                    "content": [
                        {
                            "type": "paragraph",
                            "content": [{"type": "text", "text": description}]
                        }
                    ]
                },
                "issuetype": {"name": "Task"}
            }
        }

        response = requests.post(
            url,
            json=payload,
            auth=(self.email, self.api_token)
        )

        if response.status_code == 201:
            return response.json()['key']
        else:
            raise Exception(f"Jira 티켓 생성 실패: {response.text}")

    def update_status(self, issue_key: str, status: str):
        """티켓 상태 변경"""
        # TODO: 구현
        pass
```

#### 3. workflow.py에서 사용

```python
# workflow.py의 stage1_planning 함수 수정
def stage1_planning(self, feature_description: str) -> str:
    # ...

    # Jira 클라이언트 초기화
    from lib.jira_client import JiraClient
    jira = JiraClient(self.config['jira'])

    # 실제 티켓 생성
    jira_key = jira.create_issue(
        summary=f"[Mathiter] {feature_description}",
        description=f"기능 설명: {feature_description}"
    )

    return jira_key
```

---

## 실제 사용 예시

### 예시 1: 커리큘럼 기능 추가 (실제로 했던 작업)

```bash
$ python3 workflow.py "SAT, IGCSE 커리큘럼 데이터베이스 정리 및 웹 노출"

# Stage 1: 기획
→ Claude와 논의: "커리큘럼 서비스 구조는?"
→ Jira 티켓: KAN-XXX 생성

# Stage 2: 개발
→ Claude에게 요청: "커리큘럼 서비스 구현해줘"
→ 생성된 파일:
  - backend/services/curriculum_service.py
  - backend/routes/api_routes.py (수정)
  - backend/data/sat_math_curriculum.json
  - backend/data/igcse_math_curriculum.json
  - backend/scripts/upload_curriculum.py
  - frontend/curriculum.html

# Stage 3: 리뷰
→ 보안 리뷰: XSS, SQL Injection 없음 확인
→ 품질 리뷰: 타입 힌팅, Docstring 확인

# Stage 4: 승인
→ 로컬 테스트 완료
→ 승인: y

# Stage 5: 배포
→ Git 브랜치: feature/kan-xxx-커리큘럼-기능
→ 커밋 & 푸시
→ Cloud Run 배포
→ Jira → Done
```

### 예시 2: 간단한 버그 수정

```bash
$ python3 workflow.py "Firebase 초기화 중복 방지"

# Stage 1-2: 빠르게 진행
# Stage 3: 리뷰
→ 변경 파일: backend/utils/firebase_client.py
→ 리뷰 통과

# Stage 4: 승인
→ y

# Stage 5: 배포
→ feature/kan-xxx-firebase-fix
→ 푸시 완료
```

---

## 현재 Workflow의 한계점

### 완전 자동화되지 않은 부분

1. **Jira API 연동** - 티켓 생성/업데이트가 수동
2. **Confluence 문서화** - 수동 요청 필요
3. **Cloud Run 배포** - workflow에 포함되지 않음
4. **테스트 자동 실행** - 포함되지 않음

### 개선 가능한 부분

1. **완전 자동화**
   - Jira API 완전 연동
   - Confluence API 연동
   - CI/CD 파이프라인 추가

2. **테스트 단계 추가**
   ```python
   def stage3_5_testing(self):
       # pytest 자동 실행
       # 커버리지 체크
       pass
   ```

3. **배포 자동화**
   ```python
   def stage5_deployment(self):
       # Cloud Run 배포
       # Firebase Hosting 배포
       # 헬스 체크
       pass
   ```

---

## 문제 해결

### Q1. "workflow.py를 찾을 수 없습니다"

```bash
# 현재 위치 확인
pwd

# projects 폴더로 이동
cd ~/projects

# 또는 절대 경로 사용
python3 /Users/justinminim4/projects/workflow.py "기능"
```

### Q2. "config 파일이 없습니다"

```bash
# config 폴더 확인
ls -la config/

# config 파일이 없다면 기본 설정 사용됨
# 또는 수동으로 생성:
mkdir -p config
# config/workflow_config.json 파일 생성
```

### Q3. "Jira 티켓이 실제로 생성되지 않습니다"

→ **정상입니다**. 현재는 더미 티켓(KAN-XXX)만 반환합니다.
→ 실제 연동이 필요하면 `lib/jira_client.py` 구현 필요

### Q4. "Stage 4에서 승인했는데 배포가 안 됩니다"

Stage 5의 각 단계에서 추가 확인이 필요합니다:
1. "Git 커밋을 생성하시겠습니까?" → `y`
2. "GitHub에 푸시하시겠습니까?" → `y`

---

## 다음 단계: Workflow 개선

workflow를 더 강력하게 만들려면:

### 1단계: Jira API 완전 연동
```bash
# lib/jira_client.py 구현
# workflow.py 수정
```

### 2단계: 테스트 자동화
```python
# pytest 추가
# 커버리지 리포트
```

### 3단계: 배포 자동화
```bash
# Cloud Run 배포 스크립트
# Firebase Hosting 배포
```

### 4단계: 알림 시스템
```python
# 이메일 알림
# Slack 통합
```

---

**작성일**: 2025-11-13
**마지막 업데이트**: 2025-11-13
