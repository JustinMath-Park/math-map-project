# 🏠↔️🏢 집과 회사를 오가는 개발 워크플로우

이 가이드는 집과 회사(또는 다른 두 환경)에서 끊김 없이 작업을 이어서 할 수 있도록 돕는 워크플로우입니다.

---

## 🚀 1. 회사 컴퓨터 초기 설정 (최초 1회)

회사 컴퓨터에 프로젝트를 처음 세팅할 때만 수행합니다.

### 1.1 프로젝트 가져오기 (Clone)
```bash
# 원하는 작업 폴더로 이동
cd ~/projects

# GitHub에서 프로젝트 복제
git clone https://github.com/JustinMath-Park/math-map-project.git
cd math-map-project
```

### 1.2 가상환경 설정
```bash
# backend 폴더로 이동
cd backend

# 가상환경 생성
python3 -m venv venv

# 가상환경 활성화 (Mac/Linux)
source venv/bin/activate
# (Windows의 경우: venv\Scripts\activate)

# 라이브러리 설치
pip install -r requirements.txt
```

### 1.3 🔑 중요: 보안 파일 옮기기 (수동 작업)
`.env` 파일과 Firebase 키 파일(`*.json`)은 보안상 Git에 올라가지 않습니다. **반드시 별도로 옮겨야 합니다.**

1.  **집 컴퓨터에서**:
    *   `backend/.env`
    *   `backend/your-service-account-key.json` (또는 사용 중인 키 파일)
    *   위 파일들을 USB, 개인 이메일, 또는 보안 메모(Notion 등)를 통해 복사합니다.
2.  **회사 컴퓨터에서**:
    *   `backend/` 폴더 안에 위 파일들을 붙여넣습니다.

---

## 🔄 2. 매일의 작업 루틴

### ☀️ 아침: 작업 시작 전 (Pull)
작업을 시작하기 전에 **반드시** 집에서 한 작업을 받아와야 합니다.

```bash
# 1. 프로젝트 폴더로 이동
cd ~/projects/math-map-project

# 2. 최신 코드 받기
git pull origin main

# 3. 백엔드 가상환경 켜기
cd backend
source venv/bin/activate
```

> **Tip**: 만약 `git pull` 중 충돌(Conflict)이 발생하면, 당황하지 말고 충돌난 파일을 열어 수정 후 다시 커밋하세요.

### 🌙 저녁: 작업 종료 전 (Push)
퇴근하거나 작업을 마칠 때는 **반드시** 작업 내용을 올려두어야 합니다.

```bash
# 1. 변경된 파일 확인
git status

# 2. 모든 변경사항 담기
git add .

# 3. 커밋 메시지 작성 (오늘 한 일 요약)
git commit -m "Feat: 회사에서 레벨 테스트 UI 수정 완료"

# 4. GitHub에 올리기
git push origin main
```

---

## 📦 3. 라이브러리 추가했을 때

새로운 패키지(예: `pandas`)를 설치했다면, 반드시 `requirements.txt`도 업데이트해서 Git에 올려야 합니다. 그래야 다른 컴퓨터에서도 에러가 안 납니다.

```bash
# 1. 패키지 설치
pip install pandas

# 2. 목록 업데이트
pip freeze > requirements.txt

# 3. Git에 올리기
git add requirements.txt
git commit -m "Chore: pandas 패키지 추가"
git push origin main
```

**반대쪽 컴퓨터에서 작업 시작할 때:**
```bash
git pull origin main
pip install -r requirements.txt  # 업데이트된 목록 설치
```

---

## 🚨 4. 문제 해결 (Troubleshooting)

### Q. `git pull`을 안 하고 작업을 시작했어요!
A. 작업을 조금이라도 했다면 `git stash`로 임시 저장하고 pull을 받으세요.
```bash
git stash       # 내 작업 임시 저장
git pull        # 최신 코드 받기
git stash pop   # 내 작업 다시 가져오기
```

### Q. `ModuleNotFoundError`가 떠요.
A. `requirements.txt`가 변경되었을 수 있습니다. 다시 설치해 보세요.
```bash
pip install -r requirements.txt
```

### Q. Firebase 관련 에러가 나요.
A. 키 파일(`json`)이나 `.env` 파일이 회사 컴퓨터에 제대로 복사되었는지 확인하세요. 이 파일들은 Git으로 동기화되지 않습니다.
